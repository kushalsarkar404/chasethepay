export const metadata = {
  title: "Sent emails",
  description: "View reminder emails sent to customers",
};

export default function SentEmailsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
